# 技术栈说明（nova-admin + nove-admin-nest）

> 本文档描述本仓库前后端实际使用的技术，以双方 `package.json` 为准。

## 一览

| 端 | 项目目录 | 语言/包管理 | 本地地址 |
|---|---|---|---|
| 前端 | `nova-admin`（v0.9.18） | TypeScript + pnpm | http://localhost:9980 |
| 后端 | `nove-admin-nest`（v1.0.0） | TypeScript + pnpm | http://localhost:3001（文档 /api-docs） |
| 数据库 | PostgreSQL 17（库 `nova2`） | — | localhost:5432 |
| 缓存/会话 | Redis（ioredis 5 客户端） | — | 127.0.0.1:6379 |

## 前端：Vue3 中后台模板

- **Vue 3**（`<script setup lang="tsx">`，组合式 + TSX 混写）+ **Vite 7** + **TypeScript**
- **Naive UI** + **pro-naive-ui**：后台主体组件与高级布局
- **Vue Router**（web history）：静态/动态两档路由，`VITE_ROUTE_LOAD_MODE` 切换，动态模式下侧边栏由后端 `sys_menu` 驱动
- **Pinia**（+ persistedstate 持久化）：token、用户信息、路由表、字典
- **alova**（fetch 适配器）：取代 axios，`src/service` 封装，含 token 自动附加与过期处理
- **UnoCSS**（原子化）+ **SCSS**（modern API）
- **ECharts** / **md-editor-v3** / **Quill**：图表与编辑器
- **vue-i18n**：中 / 英 / 泰 / 马来 / 德 5 语（含 Naive 对应语言包），右上角切换
- **工具库**：@vueuse/core、radash、colord
- **工程化**：unplugin（auto-import / components / icons，`useXxx` 免手写 import）、eslint + prettier、vue-tsc 类型检查

## 后端：NestJS RBAC 脚手架

- **NestJS 11**：装饰器驱动，模块划分 `auth`（登录鉴权）/ `system`（用户·角色·菜单·部门·字典）/ `monitor`（登录日志·服务状态）/ `common/redis`
- **TypeORM 0.3 + PostgreSQL 17**：实体即表，蛇形命名策略（`nickName` ↔ `nick_name`），开发环境 `synchronize` 自动建表
- **Redis**：登录会话 `user_token:{accessToken}`（7 天过期），登录写、鉴权读、登出删
- **认证**：@nestjs/jwt（载荷仅 `userId`，7 天有效）+ 自研双守卫——JwtGuard 验票、AuthGuard 验权限码，`roleKey === 'admin'` 超管直通
- **class-validator + 全局 ValidationPipe**：DTO 装饰器校验入参
- **统一返回体**：`{ code, data, message }`（HTTP 永远 200，业务成败看 `code`：200 成功 / 500 业务失败 / 401 未登录或会话过期）
- **nest-winston** 日志、**@nestjs/swagger** 接口文档（`/api-docs`）、svg-captcha（默认关闭）、geoip-lite + ua-parser-js（登录日志的地点/浏览器识别）

## 数据流转（一次登录）

```
前端 9980（alova）→ 后端 3001（Guard → Controller → Service → PG/Redis）
  → { code:200, data:{ accessToken, role } } → 存 Pinia/localStorage → 跳工作台
```

## 本地启动顺序

Redis → PostgreSQL(5432) → 后端(3001，`pnpm start:dev`) → 前端(9980，`pnpm dev`），账号 `admin / 123456`。
局域网访问：前端请求地址跟随页面主机名，后端监听 `0.0.0.0`，放行防火墙 TCP 3001 即可。